node_modules
dist
build
.next
.turbo
coverage
package-lock.json

# The spec documents are authored prose, not code. Prettier reflows markdown
# tables, which produces enormous diffs on documents that change rarely and are
# read constantly. Leave them exactly as written.
docs/


# Prettier has no SQL parser. Migrations are hand-formatted, and the
# comments in them document compliance decisions worth reading as written.
*.sql
